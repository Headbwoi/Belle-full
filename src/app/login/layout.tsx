export const metadata = {
  title: "Login - Belle Full",
  description: "Meal Application, A key to fine dining",
}

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
